<!DOCTYPE html>
<html class="h-full bg-gray-100">
  <head>
    <title>ProjectPlanner</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full">
    <div class="min-h-full">
      <div class="bg-indigo-600 pb-48">
        <nav class="print:hidden border-b border-indigo-300 border-opacity-25 bg-indigo-600 lg:border-none">
          <div class="mx-auto max-w-7xl px-2 sm:px-4 lg:px-8">
            <div class="relative flex h-16 items-center justify-between lg:border-b lg:border-indigo-400 lg:border-opacity-25">
              <div class="flex items-center px-2 lg:px-0">
                <div class="flex-shrink-0 text-white">
                  <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-10 h-10">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M8.25 21v-4.875c0-.621.504-1.125 1.125-1.125h2.25c.621 0 1.125.504 1.125 1.125V21m0 0h4.5V3.545M12.75 21h7.5V10.75M2.25 21h1.5m18 0h-18M2.25 9l4.5-1.636M18.75 3l-1.5.545m0 6.205 3 1m1.5.5-1.5-.5M6.75 7.364V3h-3v18m3-13.636 10.5-3.819" />
                  </svg>
                </div>
                <div class="lg:ml-10">
                  <div class="flex space-x-4">
                    <% main_menu.each do |menu_item| %>
                      <%= link_to menu_item.label, menu_item.path, class: menu_item.css_classes %>
                    <% end %>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </nav>
      </div>

      <main class="-mt-36 print:-mt-48">
        <div class="mx-auto max-w-7xl px-4 pb-12 sm:px-6 lg:px-8 print:p-0">
          <div class="rounded-lg bg-white px-5 py-6 shadow sm:px-6 print:shadow-none print:p-0">
            <%= yield %>
          </div>
        </div>
      </main>
    </div>

    <div aria-live="assertive" class="pointer-events-none fixed mt-16 inset-0 flex items-end px-4 py-6 sm:items-start sm:p-6">
      <%= turbo_frame_tag 'flash', class: 'flex w-full flex-col items-center space-y-4 sm:items-end' do %>
        <%= render 'partials/flash' %>
      <% end %>
    </div>

    <%= turbo_frame_tag "modal" %>
  </body>
</html>
