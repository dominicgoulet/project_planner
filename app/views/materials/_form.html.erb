<%= form_with(model: material, local: true) do |form| %>
  <div class="shadow rounded-lg border overflow-hidden">
    <div class="grid gap-8 p-6 ">
      <div>
        <%= form.label :supplier_id, class: 'block text-sm font-medium leading-6 text-gray-900' %>

        <div class="mt-2">
          <%= form.select :supplier_id, suppliers_select_options, {}, class: 'block w-full rounded-md border-0 py-1.5 text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 placeholder:text-gray-400 focus:ring-2 focus:ring-inset focus:ring-indigo-600 sm:text-sm sm:leading-6' %>
        </div>
      </div>

      <div>
        <%= form.label :name, class: 'block text-sm font-medium leading-6 text-gray-900' %>

        <div class="mt-2">
          <%= form.text_field :name, class: 'block w-full rounded-md border-0 py-1.5 text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 placeholder:text-gray-400 focus:ring-2 focus:ring-inset focus:ring-indigo-600 sm:text-sm sm:leading-6' %>
        </div>
      </div>

      <div>
        <%= form.label :cost, class: 'block text-sm font-medium leading-6 text-gray-900' %>

        <div class="mt-2">
          <%= form.text_field :cost, class: 'block w-full rounded-md border-0 py-1.5 text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 placeholder:text-gray-400 focus:ring-2 focus:ring-inset focus:ring-indigo-600 sm:text-sm sm:leading-6' %>
        </div>
      </div>

      <div>
        <%= form.label :url, class: 'block text-sm font-medium leading-6 text-gray-900' %>

        <div class="mt-2">
          <%= form.text_field :url, class: 'block w-full rounded-md border-0 py-1.5 text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 placeholder:text-gray-400 focus:ring-2 focus:ring-inset focus:ring-indigo-600 sm:text-sm sm:leading-6' %>
        </div>
      </div>
    </div>

    <div class="bg-gray-100 flex gap-4 justify-end p-6 ">
      <%= link_to 'Cancel', materials_path, class: 'rounded-md bg-white px-3 py-2 text-sm font-semibold text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 hover:bg-gray-50' %>
      <%= form.submit class: 'rounded-md bg-indigo-600 px-3 py-2 text-sm font-semibold text-white shadow-sm hover:bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600' %>
    </div>

    <%= render partial: 'partials/model_errors', locals: { model: material } %>
  </div>
<% end %>
